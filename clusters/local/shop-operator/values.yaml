ingress:  # stvari koje su u helm charts default ovde su popunjene
  baseDomain: localhost
  urlPort: ":8080"

grafana:
  url: http://kube-prometheus-stack-grafana.monitoring
  adminUser: admin
  existingSecret: grafana-admin

clusterAlerts:
  enabled: true
  discord:
    guildId: "1508273714897092648"
    botTokenSecret:
      name: discord-bot-token
      namespace: shophub
